{
  "name": "mart-obj-to-sass",
  "version": "1.1.2",
  "description": "This package provides a function to generate Sass files based on specific options",
  "main": "dist/src/index.js",
  "module": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "scripts": "pnpm run tsc && cls && node ./dist/scripts/objToSass",
    "build": "pnpm run tsc",
    "tsc": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
